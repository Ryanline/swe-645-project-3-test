# Copy this file to .env for local development and replace placeholder values as needed.
# For AWS deployment, these values will move into Kubernetes secrets and Helm values.
DATABASE_URL=sqlite:///./surveys.db
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
